---
name: resume-customization
description: 根据目标 JD 路由到财经研究型或互联网业务型简历体系，研究岗位、制定竞争策略、从事实库取材、调用高质量表达库并生成一页 PDF 简历。当用户要求定制、改写或生成针对岗位的简历时使用。
---

# 简历定制

## 目标

生成的不是“关键词匹配版简历”，而是一份有明确竞争策略的一页说服材料。

核心问题只有三个：

1. 目标岗位真正用什么标准筛人？
2. 为什么应该面这个候选人，而不是背景更标准的人？
3. 哪组真实证据最能让这个判断成立？

## 文件职责

- `通用硬规则.md`：始终遵守的结构、语言和排版约束。
- `事实库.md`：候选人经历的主要记录。优先从这里取经历、数字、责任边界和禁止项。
- `finance/简历要求.md`：财经研究型岗位的解释与选材偏好。
- `finance/高质量表达库.md`：财经成熟表达锚点。
- `internet/简历要求.md`：互联网业务型岗位的解释与选材偏好。
- `internet/高质量表达库.md`：互联网成熟表达锚点。
- `templates/resume_template.py`：Python 渲染器，`--theme finance` / `--theme internet` 两种版式。
- `agents/company_research_agent.md`：需要当前公司、团队、录用画像或招聘现实信息时调用；只做外部研究。
- `agents/resume_quality_reviewer.md`：候选简历完成后调用；只做独立审查，不直接改稿。
- `self_evaluation/SKILL.md`：只有用户要求网申自评/自我描述时调用。

**不要一次加载两个领域表达库。不要为了“充分”把整个系统上下文全部读入。按需检索。**

## Step 0｜读取顺序

1. 读取本 Skill；
2. 读取 `通用硬规则.md`；
3. 读取目标 JD；
4. 若岗位判断依赖当前公司 / 团队 / 招聘信息，调用 `agents/company_research_agent.md`，将其结构化结论作为外部证据输入；
5. 完成初步路由后，只读取对应领域 `简历要求.md`；
6. 根据候选证据计划，从 `事实库.md` 检索相关经历；
7. 只读取对应领域表达库中相关经历的章节；
8. 最后确认 `templates/resume_template.py` 对应主题的用法。

如果用户提供了新信息，先更新经历判断，再生成简历；不要让旧表达覆盖新信息。

## Step 1｜拆解 JD

内部生成以下结构，不需要机械照抄给用户：

### 工作对象
用户、商家、内容、商品、客户、被投企业、财务数据、风险规则、供应商、管理层等。

### 能力定位
判断执行 / 分析 / 协调 / 决策 / 管理的主次。不要强迫只选一个。

### 核心任务
入职后最高频的 3–5 类真实工作。

### 关键产出
岗位最终交付什么，而不是 JD 写了多少职责。

### 隐含 KPI
主管真正关心的结果。它是推断，必须标注为推断，不把所有产品岗都理解为增长、所有研究岗都理解为报告数量。

### 硬门槛与加分项
学历、专业、毕业时间、语言、证书、工具、行业/实习经验。

### 业务痛点
用 3–6 个短语回答“这个岗位为什么存在”。

### 团队文化
优先级：JD 明确信号 > 公司/招聘官方信息 > 可信外部资料 > JD 文风弱推断。
文化只影响约最后 10% 的表达风格，不用无证据软能力迎合文化。

### 理想候选人
描述最标准的录用画像：教育、实习、工具、行业与成果。

需要当前公司/招聘信息时联网。官方来源优先，社区经验只能作为补充。

## Step 2｜路由：财经研究型 or 互联网业务型

**按工作逻辑路由，不按公司行业路由。**

选择财经研究型，如果岗位最终主要评价：

- 行业/公司/政策研究；
- 财务预测、收入利润、投资判断；
- 尽调、估值、风险、合规；
- 正式研究材料或金融客户价值；
- 经营/战略岗位但核心仍是财务与产业判断。

选择互联网业务型，如果岗位最终主要评价：

- 用户/商家/内容/商品理解；
- 产品需求、运营动作、营销、增长；
- 业务指标、用户行为、数据分析；
- 产品/流程/系统方案及持续迭代；
- 制造业市场、产品、数字化、国际业务等业务型任务。

混合岗位只选一个主体系。判断标准：

> 面试官最终会用哪一套问题评价“这个人能不能干活”？

必要时允许从另一体系**借一个具体表达单元**，但不得同时加载两个大表达库。

## Step 3｜竞争策略，而不是直接改简历

先回答：

### 标准候选人是谁？
岗位更常见的候选背景是什么。

### 候选人的相对位置
分别列：
- 直接优势；
- 可迁移优势；
- 明显缺口；
- 不值得暴露的无关缺口。

### Winning Thesis
用一句内部语言回答：

> 为什么招聘者应该面这个人，而不是一个背景更标准的候选人？

再确定 2–3 个 Supporting Signals。

Winning Thesis 不一定写进页眉；它用于控制整份简历的选材和解释。

## Step 4｜Evidence Map

为岗位最重要的 3–5 项能力建立：

| 岗位能力/任务 | 最强经历证据 | 次强证据 | 类型 | 缺口处理 |
|---|---|---|---|---|
| ... | ... | ... | 直接 / 可迁移 / 弱 | ... |

规则：

- 证据来自事实库；
- 不用没有依据的能力词填坑；
- 缺口可以省略、弱化、用可迁移能力解释，但不能发明经历。

## Step 5｜给每段经历分配唯一主要信号

在写 bullet 前先形成内部计划，例如：

> 券商研究实习 → 经营/研究判断  
> 咨询/产业研究 → 需求与产业传导  
> 硕士论文 → 用户/数据/因果  
> AI 辅助研究工具 → AI 系统设计  
> 行业跟踪项目 → 信息筛选/持续复盘

实际分配由 JD 决定。

如果两个项目重复证明同一能力，保留更强者，或让另一个承担不同信号。

## Step 6｜选材

使用“组合优化”而非逐句关键词匹配。

一段经历进入简历，应至少满足一个条件：

- 对核心 JD 有直接证明力；
- 能提供其他经历无法替代的强结果/规模/差异化；
- 能弥补候选人在标准背景上的关键缺口。

一旦进入，写到其真实强度上限。
不要因为相关性较弱而把强经历写弱。

## Step 7｜调用高质量表达库

先查对应方向表达库，再决定是否改写。

### 改写权限

**L0 锁定：**公司、职位、日期、项目标题、数字、成果性质、责任边界、技术能力边界。
**L1 受控：**标签、句序、背景压缩、岗位术语翻译、删次要细节。
**L2 自由重构：**选择哪几个事实组合成 bullet、经历保留与排序、技能与课程组合。
**L3 禁止：**创造岗位关键词对应的虚假经历或结果。

改写成熟表达前必须能说明理由。没有明确收益时保持原句。

## Step 8｜写作

- 不机械套 STAR。
- 默认采用“问题/任务 → 关键动作/机制 → 最强可信证据”。
- 关键词自然嵌入经历，不逐字复制 JD。
- 每段最多 4 点。
- 每点先有清楚的主题标签。
- 正文优先写强证据，不写无证据软能力。
- 技能栏只补正文没有证明的能力。
- 教育课程按岗位选择，不为填版面堆课程。

## Step 9｜Style QA

内容完成后冻结事实和核心证据，只做局部语言 QA：

- 去套话；
- 去重复；
- 去不自然并列；
- 去无证据软能力；
- 检查行业语言是否正确；
- 检查每段经历的主要信号是否清楚。

不要再发起一次“全文去 AI 重写”。

## Step 9.5｜独立质量 Reviewer

候选简历正文完成、核心内容冻结后，调用 `agents/resume_quality_reviewer.md`。

传入：

- JD；
- Winning Thesis 与 Supporting Signals；
- Evidence Map；
- 当前候选简历；
- 本次使用到的事实库片段；
- 对应领域简历要求；
- 如有，公司研究 Agent 结论；
- 如有，上一版或高质量表达基准；
- 如已渲染，PDF / 页面截图。

Reviewer 只能输出审查报告，不直接修改简历。

处理规则：

- `PASS`：继续最终 QA；
- `PASS WITH MINOR FIXES`：主 Agent 只做局部修正，不重新生成全文；
- `REVISE`：主 Agent 按 Reviewer 的优先级修订，并至少复核所有 Major 问题；
- `REJECT`：先解决事实 / 责任 / 严重岗位错位问题，再进入后续流程。

Reviewer 的意见不是最高权威；与事实库冲突时事实库优先，与用户明确要求冲突时用户要求优先。主 Agent 对最终修改负责。

## Step 10｜渲染与视觉 QA

使用对应主题渲染：

```powershell
python templates/resume_template.py --input resume_master.json --output output/resume.pdf --theme <finance|internet>
```

先按领域执行版面分支：

- **互联网业务型：**模板排版参数锁定，不得修改；渲染后 PDF 按 `pdftotext -layout` 提取的非空文本行不得少于 53 行。版面不足只能补充或扩写真实经历。
- **财经研究型：**无固定 PDF 行数；允许按“内容价值 → 分点组合 → 行距/段距 → 页边距 → 字号”的顺序调整，字号最后处理。Python 模板中主要表现为内容取舍，超出一页时优先精简选材。

渲染后检查：

- 1 页；
- 无溢出、截断、乱码；
- 首屏层级清楚；
- 最强信息能在 10–15 秒扫描中被看到；
- 页面密度均衡；
- 底部没有明显大块无意义空白。

财经研究型简历如果空白过大：

1. 先检查是否删掉了本应保留的强内容；
2. 再调整分点组合；
3. 再调整段距和行距；
4. 最后才动字号。

模板源文件行数不作为任何体系的验收口径。

## Step 11｜最终检查

输出前检查：

- 数字和责任词都有出处；
- Agent / Skill 大小写正确；
- 固定项目标题正确；
- 日期统一；
- 同一章节按时间倒序；
- 每段 ≤4 点；
- 没有重复信号；
- 最强结果没有因跨方向投递而被弱化。

## 默认交付

当用户要求“定制简历”时，默认交付：

1. 路由结果与 Winning Thesis 的简短说明；
2. 一页 PDF；
3. 生成 PDF 使用的 JSON 母版（写入本地 `resume_master.json`）；
4. 仅列出 3–6 项关键改动，不做冗长逐句解释。

如果用户只要求分析或修改建议，不强制生成文件。
