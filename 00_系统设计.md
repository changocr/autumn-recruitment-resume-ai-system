# 系统设计与维护说明

## 1. 最终结构

本版本采用：

- 1 个运行时内核 `AGENTS.md`；
- 1 个首次建档 Skill `deep-interview-candidate/SKILL.md`；
- 1 个选岗 Skill `job_selection/SKILL.md`；
- 1 个简历定制 Skill `SKILL.md`；
- 1 个网申自评 Skill `self_evaluation/SKILL.md`；
- 1 份按需投递文案规范；
- 1 份候选人事实文件；
- 1 份通用硬规则；
- 2 套领域要求与表达库；
- 1 套 Python 双主题渲染器；
- 2 个只读支持 Agent：公司/岗位研究与简历质量审查。

核心架构是：

> **运行时内核 + 三个按需工作流 + 一份候选人事实文件 + 两套领域 View + 两个支持 Agent**

`AGENTS.md` 只负责路由、内容源优先级和维护护栏；业务步骤由对应 Skill 负责。系统不维护同时承担事实、表达、选岗、生成、审计和变更历史的万能文件。

## 2. 内容源优先级

- **候选人经历：**只以 `事实库.md` 为准；表达库、历史简历、JD 和网络资料不能反向覆盖。
- **成熟表达：**以两套领域表达库为锚点，不因重构主动概括；成熟句子只在有明确理由时改写。
- **规则：**跨岗位规则进入 `通用硬规则.md`；领域规则进入对应领域文件；流程规则进入对应 Skill。

## 3. Prompt / Skill 设计原则

本系统故意减少“写死”的细分岗位模板，采用：

- 自动加载文件只保留跨任务最小内核，业务规则按任务触发；
- 明确目标而不是枚举所有可能；
- 按阶段执行：分析 → 路由 → 竞争策略 → 证据映射 → 生成 → QA；
- 按需加载上下文，不同时加载两套领域表达库；
- 用少量高质量表达作为锚点，不用大量正反例占上下文；
- 将经历、领域偏好、流程编排分离；
- 用最终验证约束输出，而不是在前面堆几十条微观禁令。

## 4. 表达保护机制

成熟表达改写前必须有理由：

- 适配；
- 事实修正；
- 去重复；
- 可理解性；
- 版面；
- 新证据升级。

“更简洁”“换个说法”“更像互联网”本身不足以触发重写。

## 5. 后续维护

新增经历：

1. 更新 `事实库.md`；
2. 判断是否产生新的财经/互联网强表达；
3. 仅在有必要时更新对应表达库。

新增岗位：

- 不新增一套规则文件；
- 单岗位简历由简历 Skill 拆 JD 并路由；多岗位比较由选岗 Skill 统一处理；
- 只有反复出现的新岗位语言模式，才沉淀进领域要求或表达库。

发现某个成熟表达在真实投递中表现更好：

- 优先更新表达库；
- 不把一次性岗位词写进全局规则。

新增或修改规则：

- 写入承担该职责的唯一内容源；
- 只有跨任务运行规则才进入 `AGENTS.md`；

## 6. 运行时入口

入口文件保持薄层，只负责指向唯一正文：

- `.agents/skills/deep-interview-candidate/SKILL.md` → `deep-interview-candidate/SKILL.md`；
- `.agents/skills/job-selection/SKILL.md` → `job_selection/SKILL.md`；
- `.agents/skills/resume-customization/SKILL.md` → `SKILL.md`；
- `.agents/skills/application-self-evaluation/SKILL.md` → `self_evaluation/SKILL.md`；
- `.claude/CLAUDE.md` → `AGENTS.md` 的任务路由。
